def tile_image_bits(bit_image, tile_shape=(4,4), overlap=0):
    """return tiled bit string"""

def select_tiles_by_importance(stats_per_tile, budget_tiles):
    """return tile's contribution/entropy, decide the keeped tiles"""
